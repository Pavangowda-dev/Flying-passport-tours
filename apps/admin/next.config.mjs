/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // ✅ IMPORTANT: disable standalone output
  output: undefined,
}

export default nextConfig
